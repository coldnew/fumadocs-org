# Task: Convert Org-mode #+begin_export html to MDX JSX

## Description

Handle multi-line #+begin_export html blocks in Org-mode files. Convert the HTML content inside these blocks to React-compatible JSX for MDX output.

## Dependencies

- Depends on task 0033 (HTML to JSX conversion library integration)

## Tasks

- [x] Extend the org-mode converter to handle #+begin_export html blocks
- [x] Apply HTML-to-JSX transformation to block content
- [x] Preserve block structure while converting content
- [x] Ensure JSX output is valid for MDX
- [x] Add unit tests for multi-line HTML export blocks
- [x] Update content/docs/org-mode/html.org with examples
