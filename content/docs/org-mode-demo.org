#+TITLE: Complex Org-mode Features Test
#+AUTHOR: Test Author
#+DATE: 2024-11-03
#+DESCRIPTION: Testing complex Org-mode features including math, code, and tables

* Introduction

This document tests various Org-mode features to ensure proper conversion to MDX.

* Mathematical Formulas

** Math

Basic math: 1 + 1 = 2

Inline math: $E = mc^2$

Display math:
$$\int_0^1 f(x) \, dx$$

Complex formula:
$$\lim_{x \to 0} \frac{\sin x}{x} = 1$$

#+begin_warning
This is a warning message with *bold* text and a $math$ formula.
#+end_warning

#+begin_note
This is an informational note.
#+end_note

#+begin_tip
Here's a helpful tip for users.
#+end_tip

* Code Blocks

** JavaScript Example

#+begin_src javascript
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 55
#+end_src

** Python Example

#+begin_src python
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)

print(quicksort([3, 6, 8, 10, 1, 2, 1]))
#+end_src

* Tables

** Simple Table

| Name    | Age | Occupation  |
|---------+-----+-------------|
| Alice   |  25 | Engineer    |
| Bob     |  30 | Designer    |
| Charlie |  35 | Manager     |

** Table

| Item | Value |
|------|-------|
| A    | 1     |
| B    | 2     |

* Lists and Checkboxes

** Unordered Lists

- Mathematics
  - Algebra
  - Calculus
  - Geometry
- Programming
  - JavaScript
  - Python
  - TypeScript

** Ordered Lists

1. First step
2. Second step
3. Third step

** Checkboxes

- [X] Install dependencies
- [X] Set up project structure
- [ ] Write documentation
- [ ] Test functionality

* Properties and Drawers

** Task with Properties :task:

:PROPERTIES:
:ID:       task-001
:PRIORITY: A
:Effort:   2h
:END:

This is a high-priority task that requires 2 hours of effort.

** Drawer Example

:drawer:
This is content inside a drawer.
It can contain multiple lines.
:end:

* Links and References

** External Links

Visit [[https://orgmode.org][Org-mode website]] for more information.

** Internal Links

See [[Introduction]] for an overview.

* Export Blocks

** HTML Export Block

#+begin_export html
<div class="alert alert-info">
  <strong>Note:</strong> This HTML content will be converted to JSX.
</div>
#+end_export

** Markdown Export Block

#+begin_export markdown
### Custom Markdown

This content is exported as-is without processing.
#+end_export

** Noexport Example

This content before the noexport block will be visible:

#+begin_export html :noexport:
<div class="hidden">
  This HTML block will not appear in the exported MDX.
</div>
#+end_export

And this content after will also be visible.

* Export Settings

#+OPTIONS: toc:t num:t
#+LATEX_HEADER: \usepackage{amsmath}

* Footnotes

Footnotes are supported in Org-mode.

* Special Characters

- Ellipsis: ...

* DONE Final Test

- [X] Math formulas
- [X] Code blocks
- [X] Tables
- [X] Lists
- [X] Properties
- [X] Links
- [X] Footnotes